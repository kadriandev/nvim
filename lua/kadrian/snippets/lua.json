{
  "plugin (lazy)": {
    "body": [
      "return {",
      "    \"${1:}\",",
      "    version =\"${2:}\",",
      "    dependencies = {",
      "        \"${3:}\", -- optional",
      "    },",
      "    config = function()",
      "        ${4:}",
      "    end",
      "}"
    ],
    "prefix": "plugin (lazy)"
  }
}
